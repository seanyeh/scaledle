{
  "scripts": {
    "build": "rm -rf docs && npx parcel build index.html --dist-dir=docs --no-scope-hoist --public-url=/scaledle",
    "serve": "npx parcel serve index.html"
  },
  "dependencies": {
    "mithril": "^2.2.2",
    "tonal": "^5.0.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.10.2",
    "parcel": "^2.10.2"
  }
}
